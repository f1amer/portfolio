# Portfolio Rule-Based Chat Backend (Free)

This backend powers a simple IT Support chatbot (no API keys).

## Install & Run
```bash
cd backend
npm install
npm run dev
```

Backend runs at: http://localhost:5050

## Endpoint
POST /api/chat
Body:
```json
{ "message": "my wifi is not working" }
```
